package karya.core.entities.http

/**
 * Enum class representing the protocol used for HTTP requests.
 */
enum class Protocol {
  /**
   * HTTP protocol.
   */
  HTTP,

  /**
   * HTTPS protocol.
   */
  HTTPS,
}
